export type ContainerKey = "monsterbox"; // Monster Box

export interface GContainer {
  name: string;
  g: number;
  grade: number;
  explanation: string;
  ignore: boolean;
  skin: string;
  type: string;
}
