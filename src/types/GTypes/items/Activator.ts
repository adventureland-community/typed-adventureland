export type ActivatorKey = "frozenstone"; // Frozen Stone

export interface GActivator {
  onclick: string;
  s: number;
  name: string;
  g: number;
  skin: string;
  action: string;
  explanation: string;
  type: string;
}
