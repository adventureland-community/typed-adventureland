export type TrackerKey = "tracker"; // Tracktrix

export interface GTracker {
  acolor: string;
  onclick: string;
  name: string;
  g: number;
  skin: string;
  action: string;
  explanation: string;
  type: string;
  special: boolean;
}
